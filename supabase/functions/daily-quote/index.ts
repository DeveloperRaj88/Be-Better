import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
const headers = { "Access-Control-Allow-Origin": "*", "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type", "Content-Type": "application/json" };
const fallback = ["Small steps every day lead to big results.", "Discipline is choosing what you want most over what you want now.", "Your future self is built by what you do today.", "Progress, not perfection.", "Consistency turns ordinary actions into extraordinary results.", "The secret of getting ahead is getting started.", "Success is the sum of small efforts, repeated day in and day out.", "It always seems impossible until it is done.", "Do something today that your future self will thank you for.", "You do not have to be perfect to make progress."];
serve(async (req) => { if (req.method === "OPTIONS") return new Response("ok", { headers }); try { const r = await fetch("https://api.quotable.io/random?tags=inspirational"); if (!r.ok) throw Error(); const q = await r.json(); return new Response(JSON.stringify({ quote: q.content, author: q.author }), { headers }) } catch { const day = Math.floor(Date.now() / 86400000); return new Response(JSON.stringify({ quote: fallback[day % fallback.length], author: "Be Better" }), { headers }) } });
